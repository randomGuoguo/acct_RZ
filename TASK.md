# TASK

## 0. 目标
- 基于模拟的Ydata数据，加工出各种特征，提炼数据价值。

## 1. 背景
- 我现在就职的公司是一家金融科技公司，平时的业务是从各个数据厂商采购数据，做成评分产品对外出售。
- 当前由于各个金融科技服务公司采购的数据源趋同，无独特数据，无法形成竞争优势。
- 公司数据库里存储了千万级别的信贷违约标签（0/1），目前想要将这些数据做成数据产品，希望能对我们传统的评分业务提供信息增益。
- 具体形式是统计每个用户主键（ID（身份证号）+PID（手机号）+app_dt（申请日期））对应的违约情况做成特征，然后转化成信用评分，加入到我们的主评分中。

## 2.开发环境
- 使用dl_new环境

## 3.Ydata说明
- 路径：`data\demo\y.csv`

## 4.希望分阶段实现任务
- step1：简单的黑名单产品。统计每个主键（三种口径，PID+ID/ID/PID）迄今为止是否有过违约（target=1），注意使用的是风险标签，应该结合app_dt和mob来判断，比如app_dt=20240310，mob=6m，target=1，那么实际黑名单产品标识为1的日期应该是app_dt+6m，因此对于app_dt为近几个月的应该格外注意。
- step2：可回溯的黑名单产品。希望对击中的主键，在每个app_dt都能获取到是否有过违约信息。
- step3：分口径的黑名单产品。根据Org_new_class列，分口径统计每个主键在app_dt及之前是否有过违约。
- step4：分口径的违约记录产品。希望统计每个击中主键app_dt日相关违约信息，比如近三个月违约次数，近6个月优质银行机构违约次数，近3个月欺诈次数等






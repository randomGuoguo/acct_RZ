# acct_RZ Current Status 2026-03-11

## 1. 文档目的

本文档用于记录截至 2026-03-11 的当前项目状态，便于后续智能体和人类工程师快速接手，不需要从旧任务文档和历史提交中重新拼装上下文。

## 2. 当前结论

当前项目已经完成从旧 `step1~step4` 产物视角到新主产物视角的切换。

当前主产品是：

- `blacklist_features.parquet`

当前兼容产物是：

- `step1_blacklist.parquet`
- `step2_traceback.parquet`
- `step3_org_blacklist.parquet`
- `step4_window_counts.parquet`

首版边界已经明确：

- `sample_flag` 不展开进主宽表
- 主宽表只展开稳定低基数维度

## 3. 当前完成情况

### 3.1 已落地架构

已完成以下层次化重构：

- 标准化层
- 查询协议层
- 共享事件事实层
- 聚合层
- 标签族层
- 产品装配层
- 兼容输出层

### 3.2 已落地模块

已新增或已成为主链路模块：

- `src/acct_rz/feature_product.py`
- `src/acct_rz/agg_lifetime.py`
- `src/acct_rz/agg_windows.py`
- `src/acct_rz/agg_breakdown.py`
- `src/acct_rz/agg_recency.py`
- `src/acct_rz/labels_history.py`
- `src/acct_rz/labels_window.py`
- `src/acct_rz/labels_orgtype.py`
- `src/acct_rz/labels_perftype.py`
- `src/acct_rz/labels_complexity.py`
- `src/acct_rz/product_blacklist_features.py`

### 3.3 已改造旧入口

以下入口已经接到主产品链路：

- `src/acct_rz/pipeline.py`
- `src/acct_rz/query_lookup.py`
- `run_pipeline.py`

以下旧模块已经退化为兼容包装层：

- `src/acct_rz/features_blacklist.py`
- `src/acct_rz/features_org_blacklist.py`
- `src/acct_rz/features_windows.py`

## 4. 当前主产品定义

### 4.1 主产品文件

- `blacklist_features.parquet`

### 4.2 主键粒度

- `app_dt`
- `key_type`
- `key_value`
- `PID`
- `ID`

### 4.3 已包含特征族

- lifetime history
- rolling windows
- stable org_type breakdown
- stable perf_type breakdown
- complexity features

### 4.4 当前稳定维度

机构类目：

- `bank`
- `rate24`
- `rate36`

表现类目：

- `fpd`
- `dpd`

## 5. 当前兼容策略

兼容目标：

- 不立即删除 `step1~step4`
- 保持现有测试和旧消费方可继续运行

当前映射关系：

- `step1 + step2` -> history / lifetime family
- `step3` -> grouped breakdown long form
- `step4` -> window family + stable breakdown family 子集

## 6. 当前行为契约

### 6.1 日期解析

当前 `app_dt` 支持：

- `YYYYMMDD`
- `YYYY-MM-DD`

### 6.2 未命中处理

未命中历史时：

- 保留查询行
- count 型字段返回 `0` 或稳定填充后的结果
- date 型字段返回 `null`

### 6.3 产品装配保护

当前产品装配层已经包含：

- 重复列检测
- 行数不变校验
- 按统一主键左连接

## 7. 当前测试状态

最近一次全量回归：

- 命令：`conda run -n dl_new python -m pytest -q`
- 结果：`33 passed`
- 日期：2026-03-11

已覆盖测试范围：

- 聚合层
- label family
- 主产品装配
- 兼容 step 输出
- batch pipeline
- query lookup
- CLI
- README 契约

## 8. 当前主要文档

建议阅读顺序：

1. `README_20260311.md`
2. `architecture_20260311.md`
3. `docs/plans/2026-03-11-blacklist-feature-product-design.md`
4. `docs/plans/2026-03-11-blacklist-feature-product.md`

旧文档仍有参考价值，但不应优先作为当前状态入口：

- `README.md`
- `architecture.md`
- `TASK2.md`

## 9. 当前已知限制

- 没有正式的 `blacklist_feature_detail` 长表产品
- `sample_flag` 尚未被纳入 detail 层实现
- `step3` 仍然只有 `long` 格式
- 产品配置仍以代码常量为主，未抽成外部配置

## 10. 后续建议优先级

高优先级：

1. 明确是否需要 `blacklist_feature_detail`
2. 为高基数维度设计 detail 层而不是继续扩主宽表
3. 清理旧文档口径，减少 `step1~step4` 作为主边界的误导

中优先级：

1. 将维度和窗口进一步配置化
2. 增加边界日窗口测试
3. 增加 detail 层相关查询与 batch 测试

低优先级：

1. 进一步整理缓存文件和临时目录
2. 收口 README / TASK / architecture 的旧版冗余说明

## 11. 接手时的操作建议

如果要继续做产品扩展：

1. 先看 `product_blacklist_features.py`
2. 再看相关 `labels_*.py`
3. 再下钻到 `agg_*.py`

如果要改旧 step 行为：

1. 先确认是否真的需要改兼容层
2. 优先改主产品逻辑
3. 最后修改 `features_*.py` 映射

如果要改业务定义：

1. 先从 `normalize.py` 和 `events.py` 开始

## 12. 关键文件索引

- `src/acct_rz/normalize.py`
- `src/acct_rz/events.py`
- `src/acct_rz/query_snapshot.py`
- `src/acct_rz/agg_lifetime.py`
- `src/acct_rz/agg_windows.py`
- `src/acct_rz/agg_breakdown.py`
- `src/acct_rz/labels_history.py`
- `src/acct_rz/labels_orgtype.py`
- `src/acct_rz/labels_complexity.py`
- `src/acct_rz/product_blacklist_features.py`
- `src/acct_rz/pipeline.py`
- `src/acct_rz/query_lookup.py`

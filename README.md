# acct_RZ

Offline-first Ydata default feature products.

## Test

Run:

`python -m pytest -q`

## Demo pipeline

Example:

`conda run -n dl_new python run_pipeline.py`

Custom paths:

`conda run -n dl_new python run_pipeline.py --input data/demo/y.csv --output data/result`

Implementation path:

`src/acct_rz/pipeline.py`

Core function:

`run_demo_pipeline`
